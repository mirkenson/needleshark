# Яндекс Метрика

Счётчик 112428810 подключён через общий `dist/metrika.js` на всех семи HTML-страницах, включая каталог и блог. Шаблоны `blog/base.html` и `catalog/templates/base.html` автоматически подключают его для каждой новой статьи/товара. `noscript`-пиксель сохранён. Подготовлена к разрешённой публикации 11 сентября 2026. Включены webvisor, clickmap, trackLinks, accurateTrackBounce и ecommerce dataLayer по коду владельца. Ecommerce-события покупок пока не отправляются: заявки не являются оплаченными покупками.

В кабинете Метрики для отчётов по конверсии создать цели типа JavaScript-событие:
- `ui_click`: нажатия ссылок, кнопок, checkbox и выбора файла; параметры element (data-track), page (путь).
- `form_start`: первое взаимодействие с формой на странице.
- `form_submit_attempt`: отправка валидной формы.
- `lead_submitted`: сервер подтвердил сохранение заявки. Это основная цель.

Идентификаторы элементов заданы явно в HTML, не менять их без необходимости. UTM применяются к внешним ссылкам Ozon и VK (source=needleshark.ru, medium=referral, campaign=website, content=элемент). На внутренних ссылках, mailto и кнопках UTM не используются; для них предназначены события. Настройка целей в кабинете этим изменением не выполнялась.

В событиях нет имени, контакта, текста заявки или имени файла. Форма имеет класс ym-hide-content для Webvisor. Cookie-уведомление информационное: счётчик загружается сразу по указанию владельца, кнопка «Понятно» только скрывает уведомление. Закрытие хранится в localStorage needle_cookie_notice=1; это не механизм отказа от аналитики. При очистке данных браузера уведомление появится снова.

Официальный API: https://yandex.ru/support/metrica/ru/objects/reachgoal

Домены: в действующей конфигурации Nginx проверены `needleshark.ru` и `www.needleshark.ru`, оба используют один каталог релиза. После публикации те же HTML и счётчик будут обслуживаться на обоих. Иные поддомены в конфигурации этого сайта не объявлены. Старый Sites-прототип `needle-shark-prototype.mirkenson.chatgpt.site` — отдельная площадка, не обновляется публикацией на VPS. Новые пути `/blog/<slug>/` не являются поддоменами. Настройки дополнительных адресов и фильтров в кабинете Метрики отдельно не проверены.

CTA внутри статей отправляет `blog_cta_click` с параметрами `article` (slug), `cta` (стабильный id блока), `page` (путь). URL назначения, текст материала и контакты в параметрах события не передаются. Создать соответствующую JavaScript-цель в кабинете отдельно; отправка события не доказывает покупку или подписку.

## Публикация 11 сентября: ссылки и события

`site_utils.py` размечает только внешние HTTP(S)-ссылки в тегах a: utm_source=needleshark.ru, utm_medium=referral, utm_campaign=website, utm_content=стабильная метка. Существующие query-параметры (включая заданные UTM) и fragment сохраняются. Внутренние needleshark.ru/www, mailto/tel, скрипты/изображения/пиксели не меняются. Внешние ссылки на источники статьи также получают эту схему. Генераторы наследуют обработку; `ops/prepare-public.py` финализирует все HTML и sitemap.

Обработчик клика выбирает ровно одну категорию: blog_cta_click → contact_click → outbound_click → request_open → ui_click. Для внешнего CTA статьи приоритет у blog_cta_click. Значения контактов, query целевого URL, текст форм и персональные данные в событие не передаются. form_start/form_submit_attempt/lead_submitted сохраняются; последнее событие только после серверного подтверждения. Средняя кнопка мыши для ссылок также учитывается. Автоматические возможности Метрики сохранены; они не являются повторным вызовом пользовательского reachGoal.

Кабинет 112428810 доступен. Подтверждены основной адрес needleshark.ru, «Принимать данные только с указанных адресов» и «Включая поддомены». На момент начала настройки пользовательских целей не было. Создание и итоговая публичная проверка фиксируются в PROJECT_CONTEXT.md.
