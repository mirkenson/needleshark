# Технический контекст Needle Shark

Обновлено: 14 сентября 2026 года. Это снимок подтверждённого состояния, а не замена проверке сервера.

## Цель и продукт

Сайт швейного производства технических изделий в Санкт-Петербурге. Основной сайт: https://needle-shark.ru/. Остальные варианты www и needleshark.ru перенаправляют на него. Брендовый цвет `#BE3E3E`, логотип `dist/logo.svg`. Главная: первый экран, о производстве, контроль этапов, продукция, доверие/маркетплейс, B2B, заявка, подвал. Сохранять согласованный дизайн и улучшать его итерационно.

Магазин: https://www.ozon.ru/seller/needle-shark/. Владелец сообщил 80 тысяч+ продаж, рейтинг 4.9 и 24 тысячи отзывов. Независимая проверка этих цифр не выполнена. Вымышленные цитаты покупателей не опубликованы. Изображения чехла квадроцикла, органайзера и рулонов ткани сгенерированы. Почта заявок: `info@neesha.ru`.

## Код и публикация

- Репозиторий: https://github.com/mirkenson/needleshark, основная ветка `main`.
- `dist/` — исходные публичные HTML/CSS/JS и изображения, а не одноразовый результат сборки. Сборщик не требуется.
- Локальный просмотр: `python3 -m http.server 4173 --directory dist`.
- `ops/deploy.sh` — загрузка статических файлов на VPS через `needledeploy`, новый релиз и атомарная смена ссылки.
- `ops/needle-shark.nginx.conf` — сохранённая конфигурация; перед изменением сравнить с сервером.
- `server/` — подготовленный Python обработчик, очередь SQLite, systemd unit, тесты и будущий JS формы.
- `google/Code.gs` — исходник Google Apps Script. Текущая версия в Google использует фиксированный ID таблицы и проверяет заголовки без изменения оформления; перед обновлением сверить различия.
- `.openai/hosting.json` относится к старому отдельному прототипу https://needle-shark-prototype.mirkenson.chatgpt.site/. Основной VPS через Sites не публикуется.

## Инфраструктура

VPS Rucloud: `194.87.99.98`, Ubuntu 24.04, тариф 0.5 ГБ RAM / 10 ГБ SSD, swap 1 ГБ. Nginx, firewall с портами 22/80/443. DNS домена управляется в reg.ru.

Документ root: `/var/www/needle-shark/current`; релизы: `/var/www/needle-shark/releases/`; `previous` хранит предыдущий релиз. Nginx: `/etc/nginx/sites-available/needle-shark`. Let's Encrypt обслуживает все четыре имени доменов, продление через certbot.timer; актуальное состояние сертификата проверять на сервере. 11 сентября владелец явно разрешил снятие запрета индексации; результат запуска фиксируется ниже.

SSH: `~/.ssh/needle_shark_ed25519`, пользователи `needledeploy` для релизов и `root` для администрирования. GitHub использует отдельный ключ `~/.ssh/needle_shark_github_ed25519`. Здесь указаны только пути, не секреты. На другой машине требуется настроить доступ заново.

## Заявки: подтверждённое состояние

Таблица: https://docs.google.com/spreadsheets/d/18ZR-07EYR7zBuULvTD_qVIz2IzlIMDAGk0yHeFtquvU/edit

Лист `Заявки`, заголовки: ID заявки, Дата UTC, Имя, Телефон / email, Задача, Файл в письме, Статус заявки, Уведомление. Таблица создана закрытой в папке ChatGPT аккаунта владельца.

Проект Apps Script: https://script.google.com/home/projects/1J2O8GOLm7KbrU2n9TTs-baMjG23FuaYMhC-uR-Vy2C7MIm61Cr0FH-pR/edit

Webhook: https://script.google.com/macros/s/AKfycby-E311mZp6MKBZLqoRrwv70PSMG1M63UKwf--JBi3Xf9aMZ4OA3CV9E2gajc4cRGYqVA/exec

Владелец явно разрешил публичный доступ к endpoint с проверкой секретного ключа. Он установил `SHARED_SECRET` в свойствах скрипта. Локальный секрет находится в игнорируемом `server/google-shared.key`; не читать его в вывод и не добавлять в Git.

9 сентября прямой тестовый POST к webhook вернул `ok:true` с соответствующим ID. По логике обработчика это подтверждает завершение записи в таблицу и вызова отправки письма. Фактическое получение во входящие отдельно не подтверждено. Это был тест Google напрямую, не тест формы сайта.

**Форма включена 9 сентября 2026. Служба needle-leads работает на localhost:8091 за Nginx /api/leads. Заявка из браузера с PDF прошла до Google с подтверждением отправки письма. Получение во входящие отдельно не подтверждено.**

Подготовленная архитектура: POST `/api/leads` → локальная долговечная очередь SQLite → Google Sheets + письмо. Секрет хранится только на сервере и в Apps Script. Повторы дедуплицируются по UUID. Вложения JPG/PNG/PDF до 2 МБ идут в письме; в таблице только имя файла. При сбоях Google очередь повторяет отправку. Письмо может повториться при сбое между отправкой и отметкой результата. Доставленные локальные данные очищаются через 7 дней; необработанные остаются для повторной попытки. Установка и сквозная доставка проверены.

## Последние изменения и проверки

Форма активирована в релизе 20260909T082757Z-59820, код 51ee439. Проверены 360/390/768/1024/1440 px, успешная отправка, ошибка сети, серверная валидация и отсутствие дублей. Исправлено узкое переполнение первого заголовка. Полная загрузка сторонних ресурсов иногда превышала 30 секунд; отдельный аудит скорости ещё нужен.

Добавлены реквизиты из старого сайта https://needle-shark.ru/: ИП Сергеев Даниил Игоревич, ИНН 026610829836, Санкт-Петербург. Контакты: info@neesha.ru, https://vk.com/needleshark. Юридические документы открываются на старом домене по ссылкам /user-agreement и /privacy-policy (HTTP 200 проверен). Их тексты не переписывались и не копировались на новый домен.

Форма требует пустой по умолчанию чекбокс принятия соглашения и согласия на обработку данных по политике. Сервер принимает только boolean consent=true. Факт согласия и URL документов сохраняются в локальном payload вместе с временем заявки; Google пока хранит прежние восемь столбцов. Срок хранения локальной записи — прежние 7 дней после доставки, отдельный постоянный архив согласий не настроен.

Следующие работы по желанию владельца: мониторинг очереди, резервирование заявок, аудит скорости, контентные улучшения. Индексация включена 11 сентября 2026; актуальный публичный релиз описан ниже. GitHub хранит код, а не заявки.

## Особенности локальной среды Codex

На текущем Mac системный git может требовать Xcode. Рабочий git найден в `/Users/mirkenson/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback/git`. Это особенность этой машины; не вшивать путь в проектные скрипты. Сетевые вызовы из песочницы могут требовать разрешённого расширения доступа; ошибка DNS в песочнице не доказывает неисправность Google или VPS.

## Обновление: документы, изображение и PostgreSQL — 9 сентября 2026

Документы перенесены в dist/user-agreement.html и dist/privacy-policy.html, публичные адреса /user-agreement и /privacy-policy. В тексте обновлены старые домены; остальная редакция владельца сохранена. Старый текст политики упоминает Tilda и аналитику и требует отдельной актуализации под фактические интеграции (Google Sheets/email); перенос не является юридической проверкой. Ссылки в форме и футере локальные, consent_documents у новых заявок указывает новый домен.

Изображение dist/atv-cover.png заменено предоставленным владельцем файлом « Квадрик1.png» без отдельных выступов-чехлов зеркал.

PostgreSQL 16, база needle_shark, customers/orders. Схема: server/schema.sql. Интеграция: server/crm.py, вызывается до подтверждения приёма заявки. SQLite остаётся очередью доставки. Сохранившиеся заявки перенесены через backfill_crm.py без повторных писем. Доступ DBeaver и модель данных — docs/DATABASE.md. Администратор needle_admin имеет запрошенные максимальные права, приложение использует отдельного needle_app. Секреты только вне Git. PostgreSQL слушает localhost:5432, память снижена под VPS 366 МБ.

Проверка после установки: заказ №4 сохранён из браузерной формы, связан с customer_id=1; уведомление подтверждено Google после повторной попытки. Вход needle_admin по паролю и SUPERUSER проверен. Публичный релиз документов/фото: 20260909T125526Z-62329. Семь серверных тестов проходят, включая отказ при недоступном SQL. Закрытие соединений SQLite/PostgreSQL сделано явным.

## Яндекс Метрика

Установлен счётчик 112428810 на всех семи страницах по коду владельца, общая загрузка — dist/metrika.js. Общий файл dist/analytics.js: события кликов, начала/попытки отправки формы и lead_submitted после сохранения. Внешние ссылки Ozon/VK размечены UTM; внутренние — событиями, чтобы не подменять источник трафика. Cookie-уведомление снизу информационное, закрытие запоминается в localStorage. Восемь пользовательских целей созданы 11 сентября и повторно проверены 13 сентября; список и ограничения — docs/ANALYTICS.md. Поля формы исключены из записи содержимого Webvisor через ym-hide-content. 13 сентября исправлен приём данных после смены домена: основной needle-shark.ru, дополнительный needleshark.ru, поддомены включены. Счётчики 112428810 и 106683266 объединены меткой «Needle Shark — сайт» (326059), история сохранена.

## Локальный прототип каталога — 10 сентября 2026

По запросу владельца подготовлены `/catalog/` и `/catalog/chehol-na-kvadrocikl/`. **Первая версия не опубликована**: владелец сначала вносил правки. Состояние первоначального прототипа описано ниже; последующие изменения и рабочая интеграция — в разделе «Уточнения каталога».

На странице чехла: студийная галерея, семь размеров в порядке владельца, Oxford 240D / PU 2000, сценарии хранения, подбор размера, комплектация, FAQ и обращение за партией. Цена не выдумывалась. Кнопки Ozon, Wildberries и Яндекс Маркета пока открывают пояснение о ссылке-заглушке. В первой версии форма показывала название, выбранный размер и тип обращения, проверяла заполнение без отправки. Теперь она подключена к серверу; см. ниже. Прототипные страницы не подключают Метрику и сохраняют `noindex,nofollow`.

Три студийных изображения созданы встроенным ImageGen по материалам владельца; финальные файлы `dist/catalog-assets/`, около 1,1 МБ суммарно. Промпты и происхождение: `docs/assets/atv-image-prompts.json`. Внешний вид и фактуру нужно согласовать как визуализации, а не считать генерацию подтверждением конструкции.

Данные: `catalog/products.json`. Общий генератор `catalog/render.py` пишет статический HTML без изменения главной; дополнительные зависимости отсутствуют. `order` меняет порядок карточек; `visible: false` скрывает карточку, сохраняя страницу; `seasonalNote` выводит пояснение; `sections` управляет блоками и оглавлением. Содержание блоков вынесено в данные. Подробности и этап подготовки к публикации — `catalog/README.md`.

Проверено локально в браузере на 360/390/768/1024/1440 px: отсутствие горизонтального переполнения, видимые элементы каталога и страницы товара, меню, галерея, переходы, якорь размеров, синхронизация размера с заявкой, раскрытие FAQ, три заглушки маркетплейсов. Проверена форма: обязательные поля, некорректный контакт, длинный текст, корректное заполнение, повторное нажатие, закрытие Escape и возврат фокуса. Сетевого успешного сценария нет по назначению прототипа; форма без файлов. Ошибок/предупреждений JavaScript в браузере нет; локальные изображения, ссылки, якоря и HTML проверены. Исправлено переполнение скрытых radio из-за общих правил главной для input.

Четыре теста проходят: сезонное скрытие с сохранением страницы и главной, порядок карточек, отключение размерного блока без битого якоря, замена заглушки реальной ссылкой. Синтаксис JS проверен.

На момент первой версии оставались правки владельца, реальные ссылки на карточки и связь размеров с ними, согласование фотографий и порядка Д × Ш × В, подключение новой формы, события аналитики и финальная SEO-подготовка. Подключение формы выполнено в следующем этапе. После согласования — проверенный коммит в GitHub и отдельный релиз через `ops/deploy.sh`. Индексацию не включать автоматически.


## Уточнения каталога и рабочая интеграция — 10–11 сентября 2026

Владелец одобрил структуру товарной страницы. Следующие изделия добавлять через общий шаблон с той же смысловой последовательностью; это закреплено в AGENTS.md и catalog/README.md. На странице квадроцикла добавлены транспортировка, резинка по низу, стяжка с фастексом, красный шильдик «Хит», подсказки под всеми семью размерами. Типы техники указаны как ориентир по габаритам; неподтверждённые модели не перечислялись.

В локальной главной пункт шапки «Продукция» заменён на «Каталог», доступен также на мобильном. Ссылка добавлена в подвал главной, каталога, товара и двух документов. Для изменённых стилей/скриптов добавлены версии URL, чтобы старый кеш не скрывал изменения. ops/deploy.sh теперь переносит вложенные каталоги ресурсов. Статический сайт не опубликован: остаются заглушки маркетплейсов и плашка согласования, индексация не включалась.

Серверная часть установлена на действующий VPS из проверенного и отправленного в GitHub коммита fcbdf81. Папка установки: /opt/needle-shark/releases/20260910T203023Z-5951. Перед миграцией сохранены код и закрытый дамп PostgreSQL в /opt/needle-shark/backups/20260910T203023Z-5951; список содержимого дампа прочитан, восстановление из него не тестировалось. Nginx/systemd-конфигурация не менялась, служба needle-leads активна.

В таблицу orders добавлены nullable-колонки product_slug, product_name, product_size, inquiry_type, quantity, source_path. Старые записи не переписывались, старые формы совместимы. API сначала сохраняет PostgreSQL и долговечную очередь SQLite, затем подтверждает приём; Google доставляется очередью. В существующую колонку Sheets «Задача» и письмо включаются все новые сведения. Структура и доступ Google Sheets не менялись.

Сквозной тест из браузера 10 сентября: заказ №5, UUID d917976b-db88-4669-a916-431a39ab20ca, размер 220 × 98 × 106, тип wholesale, количество 12. Все шесть новых полей прочитаны в PostgreSQL. Очередь получила подтверждение Google без неудачных попыток в 20:57:41 UTC. В браузере прочитаны A7 (тот же UUID), E7 (все товарные поля и тестовый комментарий), H7 («Отправлено») листа «Заявки». Тест явно помечен и не является заказом. Получение письма во входящие отдельно не подтверждено.

Проверки: 15 серверных тестов, 4 теста каталога, синтаксис JS и shell. В браузере проверены форма с обязательными полями и неверным контактом, длинный текст на 360 px, потеря ответа, неверный ID подтверждения, блокировка повторной отправки и успешный сценарий. Три повтора на локальном изолированном стенде использовали один UUID; тестовый стенд не отправлял данные в рабочую систему. Файлов в новой форме нет; совместимость допустимого и слишком большого вложения старого API проверена серверными тестами.

После возобновления 11 сентября повторно подтверждены работа службы, прежний публичный релиз и единственная запись теста в БД. Проверены главная, каталог и товар на 360/390/768/1024/1440 px: горизонтального переполнения нет, подсказки всех размеров помещаются. Проверены переходы из шапки/подвала, меню, выбор размера, галерея, FAQ и форма. Ошибок JavaScript и сломанных изображений на каталоге/товаре не обнаружено.

Локальный просмотр для продолжения: python3 server/preview.py --port 4173 --live-api. Этот режим отправляет реальные заявки в рабочую цепочку; без --live-api отправка отключена. Сервер слушает только loopback, не содержит секретов. Публичный статический релиз при проверке оставался /var/www/needle-shark/releases/20260909T150904Z-63643, previous — /var/www/needle-shark/releases/20260909T125526Z-62329.

Остаются реальные ссылки на маркетплейсы, финальное согласование контента/совместимости, аналитика каталога и SEO-подготовка. Каталог публиковать отдельным согласованным релизом через ops/deploy.sh; после публикации проверить публичные страницы и форму повторно.


## Публичный каталог опубликован — 11 сентября 2026

Владелец прямо разрешил публикацию без ссылок на маркетплейсы. Для посетителей убрана плашка прототипа и демонстрационный диалог; площадки без URL показываются отключёнными с подписью «Пока недоступно». Рабочая форма остаётся доступна для заказа напрямую. Индексация автоматически не включается. Опубликованы главная с переходами в каталог, каталог, страница товара и ссылки в футерах.

Публичные адреса: https://needleshark.ru/catalog/ и https://needleshark.ru/catalog/chehol-na-kvadrocikl/. Код релиза f5d0514 отправлен в GitHub до установки. Установка через ops/deploy.sh: current → /var/www/needle-shark/releases/20260911T065106Z-7586; previous → /var/www/needle-shark/releases/20260909T150904Z-63643. Служба needle-leads активна, серверная логика и конфигурация Nginx не менялись.

После публикации все 18 файлов dist/ проверены по HTTPS: HTTP 200 и побайтовое совпадение с проверенной локальной версией. В браузере проверены главная, каталог и товар на 360/390/768/1024/1440 px, переход из шапки, футер, мобильное меню, галерея, выбранный размер, открытие/закрытие формы и реальная отправка. Горизонтального переполнения, новых ошибок JavaScript и сломанных изображений не обнаружено. Перед публикацией прошли четыре теста каталога, проверка синтаксиса JS и diff.

Контрольная заявка с публичной страницы: заказ №6, UUID d2a8d1d6-f065-4321-9db5-6163c3bab127, размер 225 × 120 × 120, wholesale, 20 шт. PostgreSQL хранит новые поля, очередь доставила в Google без неудачных попыток. В браузере прочитаны A8 (тот же UUID), E8 (товарные сведения и явная тестовая метка), H8 («Отправлено»). Это тест, не заказ. Получение письма во входящие отдельно не проверялось.

Остаются добавление реальных товарных URL, аналитика каталога и отдельная SEO-подготовка/включение индексации. Отсутствие URL согласовано владельцем и не блокирует опубликованный каталог. Метрика главной сохранена; товарные страницы пока без счётчика. Локальный просмотр после публикации можно запускать по server/README.md при новых правках.

## Блог подготовлен локально — 11 сентября 2026

Добавлены `/blog/`, общий шаблон статьи `/blog/<slug>/`, `blog/posts.json` и генератор `blog/render.py` без зависимостей. Пока согласованных материалов нет: опубликованный набор пуст, вымышленные статьи и авторство не добавлялись. `draft` исключается из HTML, `published` создаёт страницу и карточку. Генератор экранирует текст, проверяет адреса, даты и типы блоков; ранее сгенерированная страница удаляется при снятии публикации. Инструкция добавления материала из будущей диктовки — `blog/README.md`. Голосового ввода на сайте нет.

По уточнению владельца «Блог» добавлен в шапки и подвал всех страниц, включая юридические документы и мобильную навигацию; шаблон каталога обновлён вместе с результатом генерации. На главной перед подвалом — второстепенный блок «Популярные статьи». `featured: true` отбирает до трёх опубликованных материалов; это редакционная подборка без статистики просмотров. Генератор меняет только помеченный участок главной. Пока показано пустое состояние. Существующая форма не менялась, CTA блога ведёт на неё.

Локальные проверки: четыре теста блога (черновики/снятие публикации, экранирование, валидация, сортировка, подборка и сохранность остальной главной), четыре теста каталога, синтаксис JS и git diff. В Chrome проверены блог, главная, каталог, товар и два документа на 360/390/768/1024/1440 px: нет горизонтального переполнения, сломанных изображений и новых JS-ошибок; локальные ресурсы без HTTP-ошибок. Проверены доступность ссылки «Блог» на всех этих ширинах, мобильное меню и Escape, переходы каталог ↔ блог, CTA к форме. Длинная статья проверена через временный тестовый HTML вне dist. Новых заявок не отправляли.

На VPS изменения не опубликованы, публичная проверка блога не выполнялась. Метрика блога не подключена; noindex сохранён. Следующий этап — согласование результата и публикация проверенной версии через существующий ops/deploy.sh, затем HTTPS-проверка. Чужая папка outputs/ не изменялась и не включалась в коммит.

### Дополнение: общее покрытие Метрикой

По следующему указанию владельца счётчик 112428810 вынесен в `dist/metrika.js` с сохранением прежних настроек. Подключён ко всем шести локальным HTML-страницам и общим шаблонам каталога/блога; новые статьи получают счётчик автоматически. `analytics.js` и cookie-уведомление добавлены в блог/каталог; существующий `lead-saved` используется без изменения отправки формы. Предыдущее ограничение «Метрика блога не подключена» теперь снято в локальном коде. Компактные подвалы юридических документов также дополнены ссылкой «Блог».

Прочитана действующая Nginx-конфигурация: основной домен и www обслуживаются одним каталогом релиза; других поддоменов в конфигурации сайта нет. HTTPS-проверка curl: needleshark.ru и www.needleshark.ru — HTTP 200, отдельный закрытый Sites-прототип — HTTP 401. Поддомены не создавались. Настройки адресов/фильтров в кабинете Метрики и поступление визитов в отчёты не проверялись. На VPS новая версия по-прежнему не опубликована.

Проверки дополнения: пять тестов блога, четыре каталога; тест новой статьи подтверждает наследование счётчика. В браузере с заглушённым внешним tag.js проверены единственная загрузка/инициализация 112428810 на всех шести страницах и сохранение закрытия cookie-уведомления; это проверка интеграции, не доставки событий в отчёты Яндекса. Повторена проверка адаптива всех страниц с подключённым уведомлением.

### Дополнение: структура статьи, SEO и CTA

В общий генератор добавлены вставляемые между смысловыми блоками CTA с полями id/lead/text/label/url. Внутренние ссылки и HTTPS разрешены, небезопасные схемы и дубли id отклоняются. CTA небольшой, в существующей палитре; настоящие ссылки на маркетплейсы/каналы не выдумывались. Метрика отправляет отдельный `blog_cta_click` с article/cta/page, без URL назначения и личных данных. Цель в кабинете ещё нужно создать.

Статьи получают один H1, H2 разделов, вводный краткий ответ, необязательный видимый FAQ с H3 вопросов, canonical на основном HTTPS-домене, Open Graph и JSON-LD BlogPosting с реальными title/description/date/publisher. Опциональны отдельные seoTitle/seoDescription, согласованное имя author и фактическая updated; вымышленные авторы/изображения/рейтинги не добавляются. FAQ-разметка автоматически не создаётся. Инструкция по наполнению закрепляет естественную лексику, подтверждение фактов и смысловой переход к CTA. Индексация всё ещё закрыта; ранжирование и органический трафик не обещаются.

Проверки финального дополнения: семь тестов блога и четыре каталога, синтаксис JS, diff; отдельная временная статья вне dist проверена на 360/390/768/1024/1440 px с длинным текстом, CTA и FAQ. CTA-клик дал ровно одно ожидаемое событие в локальную очередь замоканного счётчика, затем выполнен реальный локальный переход в каталог. Рабочие события/покупки/подписки этим тестом не создавались. Реальных статей по-прежнему нет. Публикация на VPS не выполнялась.

## Первая статья и границы изменений — 11 сентября 2026

По запросу владельца локально добавлена статья `/blog/zimnee-hranenie-motocikla-i-kvadrocikla/`: подготовка бензинового мотоцикла/квадроцикла к зимнему хранению, место, мойка, обслуживание, топливо, аккумулятор, шины и возвращение к эксплуатации. Материал включён в блог и редакционную подборку главной без вымышленных просмотров. Статус published означает генерацию локального HTML; на VPS статья не опубликована, дату перед будущим релизом сверить.

Прочитаны первичные источники: Honda Storage Advice, руководство Honda Fury 2025 (уход/хранение), Polaris Storing Your ATV и руководство Sportsman 850 / XP 1000 2024, Yuasa Battery Maintenance & Storage, Shorai Battery Charging. Ссылки стоят рядом с разделами; для этого добавлен экранируемый блок sources с проверкой URL. Универсальные советы о сливе топлива, зарядке любых батарей и периодических запусках исключены. Требования конкретной модели имеют приоритет. Карточка чехла проверена на публичном сайте: Oxford 240D, PU 2000, 7 размеров, резинка, фастекс и мешок. CTA в середине ведёт на существующий товар. Ограничение Polaris на материалы с покрытием при длительном хранении явно указано рядом с CTA: товар не представлен универсальным дышащим укрытием.

По замечанию владельца проведено сравнение с исходным кодом до блога (родитель a683ad9). Подписи «Меню» и «Каталог изделий» уже существовали в мобильной навигации каталога/товара; в этой работе они не переименовывались, dist/catalog.js не менялся. Мои попутные изменения удалены: перенос навигации главной в отдельный мобильный ряд, уменьшение шрифтов/логотипа на планшетах, дополнительные ссылки в старых подвалах и лишний «Каталог» в новых шапках документов. Сохранены разрешённые ссылка «Блог» в шапках, подборка снизу главной и Метрика. Шапки главной/каталога/товара побайтово совпадают с исходными после исключения добавленной ссылки «Блог». Из старого CSS изменено только условие видимости новой ссылки; остальные добавленные стили относятся к подборке главной. На узких экранах старое поведение переноса текста сохраняется.

В AGENTS.md (раздел «Сохраняй работающий сайт») сохранено постоянное правило: любые не требуемые задачей и не согласованные изменения старых страниц, текстов, навигации, дизайна и поведения сначала конкретно объяснить и согласовать; прямо запрошенное или необходимое в согласованном объёме повторного разрешения не требует.

Проверки: 8 тестов блога, 4 каталога; Chrome — главная, блог, статья, каталог, товар, два документа на 360/390/768/1024/1440 px. Нет горизонтального переполнения, HTTP-ошибок локальных ресурсов и JS-ошибок. Проверены ссылки «Блог» в шапках/меню, переход из подборки, canonical/JSON-LD, один H1, CTA в товар и открытие/закрытие прежней формы без отправки. Событие blog_cta_click проверено в локальной очереди заглушённого счётчика, не в отчётах Яндекса. Индексация и VPS не менялись; чужой outputs/ не трогался.

## Подготовка разрешённой публикации и индексации — 11 сентября 2026

Владелец прямо разрешил публикацию блога/статьи/предыдущих правок, открытие индексации и настройку Метрики/UTM. В шаблонах удалён noindex; подготовлена Nginx-конфигурация без X-Robots-Tag. Все семь страниц имеют canonical основного домена, robots.txt разрешает обход, sitemap содержит только готовые страницы по данным каталога и опубликованные статьи. Общая UTM-обработка сохраняет query/fragment, применяется только к внешним тегам a. События кликов взаимоисключающие, данные форм и контакты не передаются.

Перед установкой прочитана реальная конфигурация и проверены active службы nginx/needle-leads. Сохранён `/etc/nginx/sites-available/needle-shark.pre-indexing-20260911`; SHA-256 копии совпал с исходником. Предыдущий current: 20260911T065106Z-7586, previous: 20260909T150904Z-63643. Кабинет Метрики доступен, домен/поддомены разрешены в настройках, цели создаются через UI. Локально проходят 8 тестов блога, 4 каталога, 2 обработки публичных ссылок; адаптив семи страниц и отдельные категории кликов проверены без дублей. Публичная установка и итоговая проверка — следующим этапом.

### Первый публичный релиз и проверка аналитики

Коммит 924cee8 отправлен в GitHub и установлен через ops/deploy.sh: current → 20260911T130815Z-11787, previous → 20260911T065106Z-7586. После сохранения резервной копии сняты только два X-Robots-Tag в Nginx, nginx -t успешен до reload, nginx/needle-leads active. Все 25 файлов dist по обоим доменам: 50 ответов HTTP 200, побайтовое совпадение, noindex отсутствует. Семь публичных страниц проверены на пяти ширинах, без переполнений и JS-ошибок.

В кабинете создано 8 точных JavaScript-целей (ID в ANALYTICS.md), настройки основного адреса/поддоменов и фильтра проверены. Реальная загрузка tag.js подтверждена на всех страницах основного домена и www; запросы watch и событий request_open/form_start/contact_click/outbound_click получили HTTP 200. При проверке перехода CTA не получено подтверждение отправки до ухода со страницы, поэтому подготовлено исправление: callback reachGoal перед обычной навигацией и ограничение ожидания 800 мс. Требуется повторная публичная проверка после следующего релиза. Заявки не отправлялись.

## Публикация завершена: блог, индексация, Метрика — 11 сентября 2026

Финальный код 749286c отправлен в GitHub и установлен через ops/deploy.sh. current → `/var/www/needle-shark/releases/20260911T131431Z-12062`, previous → `/var/www/needle-shark/releases/20260911T130815Z-11787`. Исходный релиз до блога `20260911T065106Z-7586` также сохранён. Для конфигурации Nginx сохранена проверенная копия `/etc/nginx/sites-available/needle-shark.pre-indexing-20260911`. nginx -t прошёл до reload; после установки nginx и needle-leads active, ошибок уровня err в журнале этих служб за последние 20 минут нет. Серверный обработчик заявок и база не менялись.

Публичны главная, блог, первая статья о зимнем хранении, каталог, товар и два юридических документа. На needleshark.ru и www: все 25 файлов дали 50 ответов HTTP 200 с побайтовым совпадением локальному dist; X-Robots-Tag/noindex отсутствует. Все 7 страниц имеют canonical основного HTTPS-домена; robots.txt разрешает обход, sitemap.xml перечисляет только готовые страницы. Черновиков в публикации нет. Срок появления в поиске и позиции не гарантируются.

Реальная Метрика: для каждой из 7 страниц на обоих доменах получен HTTP 200 от /watch/112428810, сопоставленный с точным page-url (14 подтверждений). tag.js загружается, счётчик единственный. Для реальных кликов/фокуса подтверждены HTTP 200 целей blog_cta_click, outbound_click, contact_click, request_open и form_start. CTA статьи проверен с обычным переходом на товар без подмены SDK; запрос цели получил 200 до перехода. Наблюдения при диагностической обёртке bootstrap-функции не используются как доказательство доставки. 8 целей с точным совпадением созданы в кабинете; ID и фильтры записаны в ANALYTICS.md. Доставка событий в endpoint не является полной проверкой всех отчётов/атрибуции. Новые заявки не отправляли, поэтому form_submit_attempt/lead_submitted в этом релизе не запускали; форма и прежняя серверная логика сохранены.

Проверены 7 публичных страниц на 360/390/768/1024/1440 px, шапки/мобильное меню, переход из подборки → статья → товар, открытие/закрытие заявки, изображения и JS: переполнений и ошибок нет. В коде взаимоисключающая классификация кликов; тесты подтверждают по одному событию, переход работает и по callback, и по таймеру при недоступном счётчике. Пройдены 8 тестов блога, 4 каталога, 2 UTM/canonical.

Внешние ссылки размечены UTM с сохранением query/fragment; внутренние и ресурсные src не меняются. VK, портал руководств Polaris и Shorai ответили 200. Honda, основной сайт Polaris и Yuasa дали 403 автоматическому клиенту, Ozon — тайм-аут; те же ограничения подтверждены на исходных URL без UTM. Это ограничение внешней проверки, не доказательство поломки ссылок. Подробный обезличенный журнал: `docs/verification/20260911-blog-launch.json`. Старые страницы вне согласованного объёма не менялись, outputs/ не затронута.

## Подготовка основного домена needle-shark.ru — 11 сентября 2026

Владелец подтвердил смену DNS. Обе записи @/www проверены: 194.87.99.98, включая авторитетный ns1.reg.ru. Прочитана реальная Nginx-конфигурация, сохранена и сверена копия /etc/nginx/sites-available/needle-shark.pre-domain-20260911. Certbot успешно расширил сертификат needleshark.ru на четыре имени: needle-shark.ru, www.needle-shark.ru, needleshark.ru, www.needleshark.ru; срок до 10 декабря 2026.

Подготовлены основной HTTPS-домен с дефисом, постоянные перенаправления остальных имён с сохранением пути/query, прежний API для уже открытых форм, новые canonical/OG/BlogPosting/sitemap/robots/UTM и адреса сайта в документах. Тексты, дизайн, CSS, JS и изображения не менялись; все изменения HTML побайтово соответствуют только замене домена. Сервер разрешает четыре точных HTTPS Origin, новые согласия ссылаются на новый домен. Пройден 31 тест (16 сервер, 8 блог, 4 каталог, 3 публикация); HTTP-тест отвергает посторонний Origin.

Публичное переключение и сквозная проверка следуют после сохранения кода. Для обновления разрешённых доменов Метрики требуется вход владельца: в текущем браузере авторизация отсутствует, запрос отправлен.

## Основной домен переключён — 11 сентября 2026

Код 4dc82fe отправлен в GitHub (codex/primary-domain). Публичный current → /var/www/needle-shark/releases/20260911T135543Z-13109, previous → /var/www/needle-shark/releases/20260911T131431Z-12062. Backend установлен из /opt/needle-shark/releases/20260911T135522Z-13098, резервная копия кода и приватный pg_dump — /opt/needle-shark/backups/20260911T135522Z-13098. Миграция колонок прошла идемпотентно без изменения существующих записей. Дамп проверен чтением оглавления; восстановление и внешнее резервирование не проверялись.

Основной адрес https://needle-shark.ru/. Три HTTPS-алиаса и все четыре HTTP-имени возвращают 301 на основной HTTPS с сохранением пути/query. Исключение — прежний API, который продолжает обслуживать POST из уже открытых вкладок. Проверены 49 комбинаций редиректов, 25 публичных файлов побайтово совпали с локальными; сертификат валиден для четырёх имён до 10 декабря 2026. certbot renew --dry-run успешно прошёл, certbot.timer active. После Nginx -t выполнен reload; nginx/needle-leads active, новых ошибок уровня err в журнале нет. SHA-256 установленной Nginx-конфигурации, общего API-фрагмента и leads.py совпали с кодом.

В браузере проверены все семь страниц на 360/390/768/1024/1440 px, canonical нового домена, отсутствие горизонтального переполнения и ошибок JavaScript. Главная визуально просмотрена на 390 px; дизайн неизменён. Реальный переход со старого домена открывает соответствующую страницу товара на новом.

Публичная форма отправила явно помеченную тестовую заявку b664f2d0-c42b-4cce-8369-7a9ba01465ef. В браузере успех показан после ответа API; очередь доставила без повторов. В закрытой Google Sheets найдено ровно одно совпадение UUID в A9, E9 содержит товар/тип/количество/путь и тестовую пометку, H9 — «Отправлено». Получение письма во входящие отдельно не подтверждалось. Новые consent_documents указывают needle-shark.ru. API для четырёх разрешённых Origin возвращает ошибку валидации на пустой объект; посторонний Origin отклонён с 403.

Осталось обновить основной/дополнительный адрес в существующем счётчике Метрики 112428810 после входа владельца в Яндекс. Сайт сохраняет тот же код и цели, но прежний фильтр адресов может исключать визиты нового домена. Авторизация в текущем браузере отсутствует; владельцу открыта вкладка входа и отправлен запрос. Успешная загрузка сайта не означает завершения переноса настроек аналитики.


## Оптимизация типографики, SEO и изображений — 11 сентября 2026

Подготовлено в ветке `codex/site-optimization`. На момент завершения локальной оптимизации публичным оставался релиз `20260911T135543Z-13109`; последующая публикация по запросу владельца описана ниже. Главная до публикации проверена read-only: HTTP 200, запрета индексации в заголовках нет, действующий robots.txt разрешает обход.

Общий `theme.css` содержит шрифт, размеры, насыщенность, цвета, линии и скругления; все текстовые правила перенесены в `typography.css` с сохранением исходных классов и адаптивных вариантов. Manrope (кириллица/латиница, веса 400–800) размещён локально в WOFF2, 39 120 байт, лицензия OFL сохранена. Убран внешний CSS @import. Вёрстка разделена на прежние компоненты и общую navigation.css; блог больше не загружает весь catalog.css. menu.js заменяет дублирование управления меню. Инструкция с ролями, токенами, размерами и правилами изменения: docs/TYPOGRAPHY.md.

Все семь страниц получают через site_utils.py единые title/description, canonical, OG/Twitter, robots и иконки. Добавлены связанные Organization/WebSite/WebPage, CollectionPage/ItemList для каталога и блога, Product с реальными параметрами и BreadcrumbList; статья сохраняет BlogPosting с датами и первичными источниками. Product без цены/отзыва не претендует на расширенный товарный результат Google. Внутренние .html/index.html-ссылки нормализованы, sitemap содержит только готовые страницы и реальную дату статьи; API исключён из обхода robots. Слоганы/названия/коммерческие данные/редакция юридических текстов сохранены. Отдельные названия разделов документов стали H2 без изменения внешнего вида. Правила и ограничения: docs/SEO.md.

Квадратные фавиконы SVG/ICO/PNG, Apple Touch Icon и manifest созданы из существующего красного n логотипа. brand-preview.png использует прежний логотип. Исходные семь PNG/JPG не изменены. 34 WebP-версии созданы в dist/images; размеры/хеши — ops/image-manifest.json; воспроизводимая обработка — ops/optimize-images.py (локальный Pillow). Самые крупные версии суммарно 786 690 байт вместо 9 601 116 байт (−91,8%). Есть srcset/sizes, маленькие миниатюры, lazy-loading, высокий приоритет первого фото. У чехла на главной исправлены реальные intrinsic-размеры 900×1124 при прежнем кадрировании. Галерея синхронно меняет src и srcset.

Проверки: 36 Python-тестов (8 блог, 6 каталог, 6 публикация, 16 сервер), статический check-site.py, синтаксис JS и diff. Chrome: 7 страниц × 5 ширин 360/390/768/1024/1440, без горизонтального переполнения, сломанных локальных ресурсов и JS-ошибок. Размеры/начертания/межстрочные/ширины текста главной, каталога и блога сопоставлены с исходной версией на 390/1440 px, совпадают. При 200% размера текста на 1440 px горизонтальной прокрутки нет. Проверены галерея, размер, FAQ, меню/Escape/фокус, статья → товар и cookie-уведомление. Формы проверены с локально подменёнными ответами API: обязательные поля, неверный контакт, допустимый и >2 МБ файл, сетевая ошибка, повтор с прежним UUID, двойной клик, успех после подтверждения и длинная ошибка. Реальные заявки не создавались, Google/email и настоящая Метрика не перепроверялись. Исправлен только локальный preview.py для extensionless-адресов документов; рабочий backend не менялся.

При одинаковом локальном Chrome 390 px / DPR 1, после прокрутки страницы, растровые ресурсы главной: 8 504 743 → 93 550 байт; товара: 1 096 373 → 66 060 байт. Это лабораторный объём ресурсов, не оценка скорости публичного VPS или полевых Core Web Vitals. Обезличенная проверка и хеши: docs/verification/20260911-optimization.json.

Локальный этап завершён до публикации; дальнейшие действия и проверка Search Console/Яндекс Вебмастера описаны ниже. Позиции, сроки индексации и цитирование LLM не гарантируются. Nginx/systemd/базы не менялись; настройка сжатия/кеша VPS и прежняя задача домена Метрики остаются отдельными. Чужая папка outputs/ не затронута.


## Публикация оптимизации и проверка индексации — 11 сентября 2026

По прямому запросу владельца проверенная ветка `codex/site-optimization` отправлена в GitHub. Оптимизация опубликована через `ops/deploy.sh` сначала из `1318f3b` (релиз `20260911T173249Z-5222`), затем из `2fad52d` с метатегами подтверждения поисковых кабинетов. Итоговый current → `/var/www/needle-shark/releases/20260911T174008Z-5491`, previous → `/var/www/needle-shark/releases/20260911T173249Z-5222`. Оба переключения атомарные; Nginx/backend не менялись, nginx и needle-leads active.

Все 73 публичных файла по HTTPS побайтово совпали с dist, без X-Robots-Tag. robots.txt разрешает сайт, sitemap содержит семь актуальных URL, вымышленных и неопубликованных страниц нет. SVG/ICO/WebP/WOFF2 отдаются успешно; manifest доступен, сервер выдаёт для .webmanifest application/octet-stream, изменение MIME-конфигурации в этот выпуск не входило. TLS проверен с системным доверенным хранилищем без отключения валидации. Старый `/motocover-landing` и заведомо отсутствующий адрес отвечают 404. Редирект `https://needleshark.ru/catalog/chehol-na-kvadrocikl/?check=redirect` → основной HTTPS возвращает 301 с сохранением пути/query.

На опубликованной оптимизации выполнены 35 браузерных проверок (7 страниц × 360/390/768/1024/1440): HTTP 200, без горизонтального переполнения, ошибок JS, сломанных изображений и локальных ресурсов. Дополнительно 22 проверки интерфейса: меню, модальное окно/Escape/возврат фокуса, cookie-уведомление, три картинки галереи, выбор размера, FAQ, статья → товар и skip-link. Нативный skip-link проверен по переходу следующего Tab к первому действию main; сам нефокусируемый main не обязан становиться document.activeElement. Аналитика подменялась только в повторных браузерных QA; реальные заявки, Sheets/email и настройки Метрики повторно не проверялись. Итоговый релиз отличается от этой вёрстки только двумя невидимыми метатегами, после переключения все файлы перепроверены по HTTPS. Отчёт: docs/verification/20260911-publication.json.

После входа владельца Яндекс Вебмастер показал 11 URL в поиске, в основном от прежнего сайта; главная посещалась 7 сентября и имела старый title. Sitemap отправлен на переобход, все семь нынешних страниц получили статус «В очереди» 11 сентября в 20:40 МСК. Дополнительный инструмент «Проверка страницы» завершился «Сервис временно недоступен»; успешная диагностика роботом Яндекса не заявляется. Восстановлен метатег действующего владельца из раздела прав доступа. Старые адреса в индексе и старые карты Tilda должны обновиться после обхода; массовые удаления/неэквивалентные редиректы не выполнялись.

В Google Search Console подтверждён нынешний HTTPS-ресурс; раньше был доступен только HTTP. Google принял sitemap, сводный отчёт нового ресурса ещё обрабатывается и предлагает повторить проверку примерно через день. Проверка URL подтвердила наличие главной в индексе, последнее сканирование 29 августа 2026, canonical совпадает с основным HTTPS. Запрос повторного индексирования главной принят в приоритетную очередь. Карточка чехла на момент проверки не проиндексирована («URL неизвестен Google»); её отдельный запрос на индексирование принят в приоритетную очередь. Новая статья о зимнем хранении также пока неизвестна Google.

Запрос индексирования статьи также принят в приоритетную очередь. Обработка sitemap в Google на момент завершения имеет ошибку «Не удалось обработать файл Sitemap» / «Не получено», 0 выявленных страниц. Отправка карты не считается успешной обработкой. Проверены XML, публичный HTTP 200, text/xml, отсутствие X-Robots-Tag, A-запись локального резолвера и Google DNS → 194.87.99.98, AAAA отсутствует. В журнале VPS запрос с Google User-Agent в 17:48:42 UTC получил 200 и все 597 байт sitemap; происхождение IP отдельно не аттестовывалось. Причина ошибки Search Console не установлена; нужна повторная проверка обработки карты после обновления отчётов. DNS/Nginx ради предположений не менялись.

Осталось дождаться нового обхода и проверить фактическое появление актуальных страниц/текстов в Google и Яндексе; постановка в очередь не означает индексирование. Для Яндекса повторить недоступную диагностику страницы, для Google — проверить ошибку обработки sitemap и новый сводный отчёт. Автоматическое наблюдение не создавалось. Обновление настроек домена Метрики остаётся отдельной незавершённой задачей. Чужая папка outputs/ не затронута.


## Исправление Метрики после смены домена — 13 сентября 2026

На публичном сайте подтверждён единственный счётчик 112428810 («Needleshark (second)»). Его кабинет по-прежнему разрешал только needleshark.ru, хотя сайт уже перенаправлял посетителей на needle-shark.ru. Дополнительных адресов не было; приём только с заданных адресов и общий фильтр по сайту исключали новый домен.

В Метрике сохранены основной адрес needle-shark.ru и дополнительный needleshark.ru; поддомены и ограничения приёма сохранены. Повторное открытие настроек подтвердило сохранение. Фильтр «Оставить только трафик → URL страницы → сайт и дополнительные адреса» оставлен; исключение собственных визитов и поведенческая фильтрация роботов выключены. Все восемь пользовательских целей имеют прежние ID. Названия счётчиков и доступы не менялись.

Второй счётчик 106683266 («Needle Shark (site)», владелец mirkenson) содержит историю прежнего сайта на needle-shark.ru; нынешняя страница его не загружает. В аккаунте mirkenson.neesha проверен существующий доступ «Редактирование». Оба счётчика включены в общую метку «Needle Shark — сайт», ID 326059. Сводка https://metrika.yandex.ru/dashboard?group=day&period=month&gid=326059 успешно открылась и показала страницы обоих доменов, в том числе прежние /motocover-landing и /motorcycle. Накопленные данные не переносились между счётчиками. Цели, Вебвизор и карты остаются раздельными согласно возможностям отчёта по группе.

Публичная проверка: семь HTML и metrika.js/analytics.js по HTTPS побайтово совпали с dist; каждое HTML содержит одно подключение metrika.js, код 106683266 отсутствует. Редирект старого домена на товар сохранил путь и query (301). В Chrome встроенная проверка Яндекса показала найденный 112428810, реальный отладчик — PageView нового домена и reachGoal form_start при фокусе в пустом поле. В общей сводке за сегодня в режиме «с роботами» уже отобразились оба проверочных просмотра нового домена: _ym_status-check и _ym_debug=2. Визит размечен utm_source=metrika_check, utm_medium=qa, utm_campaign=domain_fix_20260913. После обновления отдельного отчёта 112428810 за сегодня по цели form_start (611920074) появились 1 достижение цели и 1 целевой визит. Таким образом, подтверждены и обработка просмотра, и обработка пользовательской цели в отчётах. Это тест аналитики, заявок и контактов не отправляли.

Исправление действует в кабинете Метрики; код сайта и сервер не изменялись, ops/deploy.sh не запускался. Дизайн и форма не редактировались, повторная проверка пяти ширин/серверных тестов не требовалась. Цель lead_submitted и цепочка очередь → Sheets → письмо повторно не тестировались. Период визитов, отброшенных фильтром до исправления, сменой адреса не восстановлен. Чужая папка outputs/ не затронута. Подробности и ссылки: docs/ANALYTICS.md.


## Моточехлы, чехлы для колёс и Ozon UTM — 14 сентября 2026

Локально подготовлены `/catalog/chehol-dlya-motocikla/` и `/catalog/chehly-dlya-koles/` через действующий общий шаблон. В каталоге теперь три товара. Квадроциклы повторно не импортированы, чехлы снегоуборщиков исключены. Прежняя страница квадроцикла получает только предоставленную ссылку Ozon и соответствующую подпись о цене/доставке; её размеры, описания и композиция сохранены. На главной изменена только UTM campaign существующей ссылки Ozon.

Сверены Excel и изображения владельца: обычный моточехол S/M/L/XL, специальный с жаропрочными вставками, вентиляцией и светоотражателями только M/L. Чехлы для колёс: 1 или 4 шт., R17–R22, внешний диаметр с шиной до 800 мм. Для S выбрано 170 × 90 × 100 см по совпадающим отдельным колонкам, описанию и двум изображениям; противоречивые AW19 и отдельный текстовый файл не перенесены. Источники, точные строки, ID карточек и ограничения таблицы совместимости: `docs/catalog-ozon-20260914.md`.

Выбор построен по перечисленным сочетаниям `optionGroups`/`variants`: комплектация → размер, с возможностью добавить группу цвета. Несуществующих сочетаний нет. Переключаются ссылка Ozon, фото, выбранный комплект и заявка. Восемь вариантов ведут на восемь отдельных Ozon ID из выгрузки. Все Ozon-ссылки на сайте и в динамических данных получают `utm_campaign=vendor_org_211216`, source основного домена, medium referral и content места/варианта. Старые campaign заменяются, повторная генерация идемпотентна.

Встроенный ImageGen создал четыре отдельные студийные визуализации по изображениям владельца: обычный/специальный моточехол, один/четыре чехла для колёс. Исходники в `dist/catalog-assets/`, адаптивные WebP в `dist/images/`. Промпты: `docs/assets/moto-wheel-image-prompts.json`. Изображения визуально проверены, генерация не служит подтверждением свойств. Логотип, цвет и типографика сайта сохранены.

Backend не менялся. Детали комплектации и размера записываются в существующее `question`; мотоциклы также сохраняют габариты в прежнем `product_size`, колёса передают свою систему размера текстом. Пользователь указывает количество комплектов колёс, а `quantity` содержит итоговое количество чехлов. Проверено: 2 комплекта по 4 передают 8 шт., оба числа присутствуют в заявке. Миграция БД не требуется.

Локально прошли 40 Python-тестов (9 каталог, 7 публикация, 8 блог, 16 сервер), проверка JS и `ops/check-site.py` для девяти страниц. Chrome: 9 страниц × 5 ширин 360/390/768/1024/1440, без горизонтального переполнения, сломанных изображений, локальных HTTP-ошибок и JS-ошибок. Проверены все 8 ссылок/сочетаний, автоматический переход S→M при выборе специальной модели, сохранение L при смене комплектации, галерея и размеры прежнего квадроцикла, FAQ, меню/Escape/возврат фокуса. В форме локально подменены ответы API: обязательные поля, неверный контакт, ошибка сети, несовпавший ID ответа, длинный текст/ошибка, повтор с тем же UUID, двойное нажатие и успех только после подтверждения. Отчёт: `docs/verification/20260914-catalog-local.json`.

На VPS этот этап не опубликован; публичная версия остаётся прежней. Реальные заявки не создавались, сквозная доставка новой комплектации в Sheets/почту не проверялась. Живые карточки Ozon не открылись через веб-инструмент: текущие наличие, цена, редиректы и обработка UTM в отчётах Ozon не подтверждены. Перед публикацией: отправить проверенный коммит в GitHub, установить через `ops/deploy.sh` и проверить публичный HTTPS. Чужая папка `outputs/` не затронута.


## Раздельные галереи и детали трёх чехлов — 14 сентября 2026 (локально)

По уточнению владельца галереи привязаны к комплектации через `variants[].galleryId` и `galleries` в общем каталоге. У обычного моточехла — 5 кадров; у модели с жаропрочными вставками — 8, включая внутреннюю брезентовую вставку, вентиляционные карманы и светоотражатели. Снимки другой модели в галерею и фотографии разделов не попадают. Комплекты колёс из 1 и 4 штук имеют по 5 кадров со своим общим видом и одинаковыми деталями конструкции; у квадроцикла 6 кадров. При смене комплектации синхронно обновляются основной кадр, миниатюры, материал, фото размера и комплекта, карточки деталей и подписи. При смене только размера выбранный кадр сохраняется.

Встроенным ImageGen созданы 14 дополнительных визуализаций: нижний край, шов, фастекс для ATV и мото; ткань и три специальных элемента мото; стяжка-шнурок, шов, ручка с карманом и ткань для колёс. Добавлены адаптивные WebP. Детали показаны и в галерее, и отдельными фото с пояснениями в разделе конструкции, фактура ткани — в блоке материала. Промпты и исходники: `docs/assets/catalog-detail-image-prompts.json`. Генерация не подтверждает испытания ткани или точное фотографическое соответствие изделию.

Во всех трёх карточках каталога и товарных страницах присутствуют отключённые Wildberries и Яндекс Маркет. FAQ заменены на вопросы о посадке и комплектации, водостойкости, прочности, собственном производстве, прямом заказе и доставке. Ответ владельца: «Доставку согласуем индивидуально»; конкретные службы, сроки и стоимость не обещаны. UTM campaign Ozon сохранена: `vendor_org_211216`.

Проверено локально: 18 Python-тестов (11 каталог, 7 публикация), `ops/check-site.py` для 9 страниц, синтаксис JavaScript. Chrome: 45 проверок страниц на ширинах 360/390/768/1024/1440; дополнительно 25 проверок галерей (5 наборов × 5 ширин) с каждой миниатюрой через Enter, фотографиями и подписями всех разделов, сохранением кадра при смене размера, открытыми FAQ. Ошибок JS, локальных HTTP-ошибок, сломанных изображений и горизонтального переполнения нет. Проверены 8 вариантов/ссылок, меню, фокус и форма с локальными ответами API, включая повтор, двойное нажатие и ошибки. Отчёт: `docs/verification/20260914-catalog-details-local.json`.

Эта редакция остаётся локальной, без установки на VPS и отправки в GitHub. Реальные заявки и доставка в Google/почту не проверялись; для браузерной QA Метрика подменена. Перед публикацией отправить проверенную версию в GitHub, выполнить `ops/deploy.sh` и проверить публичный HTTPS. Посторонняя папка `outputs/` сохранена.


## Подготовка публикации каталога — 14 сентября 2026

Владелец разрешил публикацию всего подготовленного каталога и уточнил, что резинка моточехла выполнена так же, как у квадроцикла. Встроенным ImageGen заменена деталь `moto-elastic.png`: резинка внутри подогнутого края той же ткани, без открытой плетёной ленты. Исправление действует для обеих моделей. Производные WebP пересозданы; промпт и происхождение — `docs/assets/moto-elastic-correction.json`.

Перед публикацией: 26 Python-тестов (11 каталог, 7 публикация, 8 блог), `ops/check-site.py`, 25 браузерных проверок галерей на пяти ширинах и отдельная проверка счётчика/меток/восьми событий на девяти страницах прошли без ошибок. События и API в этой проверке подменены локально; реальных заявок нет. Отчёт: `docs/verification/20260914-catalog-release-preflight.json`. На сервере до публикации current=20260911T174008Z-5491, previous=20260911T173249Z-5222, nginx и needle-leads active, свободно 4,6 ГБ. Результат публикации будет записан после проверки HTTPS.


## Публикация полного каталога — 14 сентября 2026

По прямому разрешению владельца ветка `codex/catalog-detail-galleries` с коммитом `2f098b0` отправлена в GitHub и установлена через `ops/deploy.sh`. Опубликованы три товара, две новые страницы, выбор комплектаций/размеров, раздельные галереи и детали, обновлённые FAQ, заглушки Wildberries/Яндекс Маркета и UTM Ozon. Исправленная резинка моточехла показана в обеих его комплектациях.

Атомарное переключение: current → `/var/www/needle-shark/releases/20260914T080537Z-8230`, previous → `/var/www/needle-shark/releases/20260911T174008Z-5491`. Все 183 файла релиза побайтово совпали с локальным dist по SHA-256. Nginx и needle-leads active; конфигурация служб, API, базы и модель доступа не менялись.

Публичная проверка HTTPS: девять HTML-страниц совпали с локальной версией и отвечают 200, X-Robots-Tag отсутствует. robots.txt разрешает публичные страницы/ресурсы, sitemap содержит девять URL, включая оба новых товара; canonical и meta robots корректны. Оба метатега поискового подтверждения сохранены. Три доменных алиаса отвечают 301 с сохранением пути/query; отсутствующая страница — 404. Техническая готовность к обходу подтверждена; фактическое включение новых URL в индекс и ручной переобход не подтверждены.

Chrome на публичном сайте: 45 проверок (9 страниц × 360/390/768/1024/1440), дополнительно 25 проверок раздельных галерей (5 наборов × 5 ширин), все миниатюры, подписи, снимки разделов, 8 вариантов и Ozon URL, форма/ошибки/повтор с локальными ответами API, меню/Escape/фокус/FAQ. Ошибок JS, сломанных локальных ресурсов, новых 404 и горизонтального переполнения нет. Прежняя UTM `vendor_org_211216` подтверждена для статических ссылок и 8 вариантов.

На всех девяти страницах по одному подключению счётчика 112428810 и analytics.js. Все восемь пользовательских событий проверены с локальной подменой SDK; `lead_submitted` — только после подтверждения API, без персональных данных в событии. С настоящим SDK выполнены размеченные проверочные визиты (`release_check / qa / catalog_20260914`) на страницу моточехла: tag.js 200, PageView и цели `request_open`/`form_start` получили HTTP 200 от mc.yandex.com/watch/112428810/1 после штатных редиректов. Первые два прогона проверяли загрузку SDK и уточняли регистрацию сетевых редиректов, конечное подтверждение получено в третьем. Формы не отправлялись.

Определения восьми целей и разрешённые домены в кабинете последний раз подтверждены 13 сентября. Повторная проверка кабинета 14 сентября остановлена инструментом Computer Use: доступ к текущему URL Chrome запрещён. Обработка нынешних событий отчётами не подтверждена; HTTP 200 подтверждает доставку сервису, но не появление в отчёте. Реальные заявки, Google Sheets и почта повторно не проверялись. Полный обезличенный отчёт: `docs/verification/20260914-catalog-publication.json`. Посторонняя папка `outputs/` сохранена.
