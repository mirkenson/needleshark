# SEO и доступность содержания для поисковых/AI-систем

## B2B: поисковое содержание — опубликовано 24 сентября 2026

На `/business/` H1 «Швейное производство в Санкт-Петербурге», ясная специализация чехлы/сумки/ремни и доставка по России; конкретизированы два H2, описание и FAQ. Title сохранён; description, Open Graph и JSON-LD согласованы общим генератором. Один H1, canonical основного HTTPS, статический HTML, index/follow, без блокирующего X-Robots-Tag; URL один раз в sitemap из 39 адресов, robots разрешает обход.

Код `477a0d4` опубликован в `20260924T073408Z-14797`. Google URL Inspection показывает страницу в индексе; новый запрос индексирования принят в приоритетную очередь. Яндекс 24.09.2026 10:38 МСК принял URL на переобход («В очереди»). Это не подтверждение обновлённого сниппета, текста индекса или роста позиций. Следующие задачи — [беклог](BACKLOG.md); [проверки](verification/20260924-business-seo.json).

## Подборка на главной — 24 сентября 2026

Опубликованы три обычные внутренние ссылки на последние статьи вместо одной выбранной вручную. Карточки находятся в статическом HTML и доступны без JS. Главная по HTTPS: 200, один H1, прежние title/description/canonical/OG/JSON-LD, index/follow, без блокирующего X-Robots-Tag; канонический URL один раз в sitemap, robots разрешает обход и содержит sitemap. Проверка всех 39 страниц проходит, URL и тексты статей не менялись. Для этой небольшой правки подборки ручной переобход не запрашивался, фактическая индексация заново не проверялась. [Отчёт](verification/20260924-home-latest-articles.json).

## B2B: проверка 23.09.2026 после исправления пробелов

Публичный `/business/` отвечает 200, один H1, canonical `https://needle-shark.ru/business/`, index/follow, без блокирующего X-Robots-Tag. URL встречается в sitemap один раз, robots содержит ссылку на sitemap, alias-редирект сохраняет query. 49 ресурсов доступны. Разметка/контент кроме пяти пробелов не менялись; новый запрос переобхода для этой правки не выполнялся. [Отчёт](verification/20260923-b2b-heading-preflight.json).

## Общий выпуск — опубликован 23 сентября 2026

Релиз `20260923T181352Z-5967`, код `ea4b9005a9e3711ffc7f6bea436d1f13ab5f1860`: 39 публичных HTML, включая шесть новых статей с датой 2026-09-23. Проверены метаданные/JSON-LD, один H1, canonical, внутренние ссылки, robots, sitemap из 39 URL без дублей и отсутствие noindex. На публичном HTTPS подтверждены совпадение HTML с dist, отсутствие блокирующего X-Robots-Tag и рабочие ресурсы. Опубликован обычный dist; noindex-папки предпросмотра не использовались.

Яндекс Вебмастер принял все 39 канонических URL в очередь переобхода 23.09.2026 в 21:30 МСК; дневной остаток изменился с 150 на 111. Шесть новых статей показаны со статусом «В очереди». Таблица Sitemap в кабинете ещё показывала старую загрузку, кнопка повторного обхода основной карты была недоступна; публичная карта актуальна. Google Search Console открыт, но новую проверку/отправку подтвердить не удалось при переключениях активного окна Chrome. Старые результаты Google ниже не считаются новой проверкой. Фактическая индексация новых статей не подтверждена. [Отчёт](verification/20260923-ux-blog-publication.json). Следующие разделы — история до этого релиза.

## Ссылки Ozon из API — 23 сентября 2026, локально

В страницы 26 товаров добавлены подтверждённые розничные ссылки, включая отдельные SKU семи размеров квадроцикла. Canonical и адреса страниц прежние; 33 страницы проходят `check-site.py`, повторная сборка стабильна. Цены/статусы API не превращались в статические Offer/availability. Просмотр остаётся отдельной noindex-копией; публикации и отправки на переобход в этой задаче не было. [Отчёт](verification/20260923-ozon-api-mapping.json).

## UX-прототип — 23 сентября 2026, не опубликован

Проверены метаданные, единственный H1, ссылки, sitemap и JSON-LD 33 страниц рабочей сборки. Главная и описание каталога отражают расширенный ассортимент. В отдельной копии для просмотра все HTML имеют `noindex,nofollow`, robots запрещает обход. В production эта копия не предназначена. Переобход поисковикам не отправлялся; фактическая индексация не проверялась. Содержимое статей и юридических документов сохранено; в них изменены лишь версии общих CSS.

## Черновики статей — 23 сентября 2026

Шесть новых статей о выборе изделий имеют `draft`, отдельные title/description, постоянные slug и источники. В обычные `dist/`, блог и sitemap они не включены; прежние 33 страницы не изменились. Изолированный `blog/preview.py` создаёт noindex/nofollow-копию с запрещающим robots и пустым sitemap, без datePublished у черновиков. Все шесть текстов и панели доступны без JS; canonical в копии соответствует предполагаемому основному URL, но это не публикация и не подтверждение индексации. Перед выпуском установить фактическую дату и выполнить полный порядок AGENTS.md. [Отчёт](verification/20260923-blog-drafts.json).

## Публикация 26 товаров — 22 сентября 2026

Владелец разрешил публикацию 23 новых карточек. Все 26 товаров включены в обычный HTML-каталог и sitemap; всего 33 канонических URL, noindex отсутствует. Поиск не скрывает содержание от робота: ссылки и товарные данные находятся в статическом HTML. Публичные 33 страницы, robots/sitemap и отсутствие блокирующего X-Robots-Tag проверены после установки `20260922T181556Z-89524`. Яндекс принял 27 URL в очередь переобхода; автоматический обход через действующий счётчик 112428810 включён. Google принял sitemap со статусом «Успешно» и живой проверкой подтвердил доступность каталога для индексирования. Принятый запрос не равен фактическому включению новых карточек в индекс; результаты ручных запросов указаны в [отчёте](verification/20260922-catalog-publication.json).

Живая проверка новой карточки коляски: страницу можно проиндексировать, но расширенный Product требует одного из offers/review/aggregateRating. Это ограничение товарного оформления, а не запрет обычного обхода; вымышленные цены/отзывы не добавляем. Ручной запрос каталога Google вернул временную ошибку. По просьбе владельца перед выключением компьютера повтор ручных запросов и проверка обновления sitemap Яндекса перенесены на завтра; принятые 27 URL Яндекса и sitemap Google остаются действующими.

Исследование товарных фидов прикреплено к задаче «Выйти с товарами в товарный фид» в [BACKLOG.md](BACKLOG.md#выйти-с-товарами-в-товарный-фид). Публикация каталога не подменяет этот отложенный этап, цены/Offer и фиды не выдумываются.

## Навигация и исследование товарных фидов — 22 сентября 2026

Поиск и восемь категорий работают поверх статического списка: ссылки на все видимые карточки остаются в HTML, страницы фильтров и дубли URL не создаются. Новые студийные фотографии получают размеры, alt, srcset и маркер происхождения IPTC/XMP. На этапе локальной подготовки 23 новых товара были draft; после разрешения владельца обычная сборка включает все 26 товаров. Только изолированный --preview остаётся noindex.

Официальные требования Яндекс Товаров, Директа и Google исследованы в [product-feeds-20260922.md](product-feeds-20260922.md). Не хватает согласованных цен/наличия и собственного сценария покупки; форма запроса не равна корзине. Текущий Product не содержит вымышленных Offer/отзывов. Фиды ещё не созданы и не зарегистрированы, кабинеты и фактическое включение товаров в поиск не проверялись. Ограничения рекламы Google для российского бизнеса отделены от обычной индексации.

## Черновики каталога — 22 сентября 2026

23 новые карточки подготовлены локально, без публикации. Обычная генерация и sitemap пропускают `status: draft`. `catalog/render.py --preview` создаёт отдельную копию в `outputs/catalog-preview`: noindex,nofollow и robots Disallow. Основные canonical/OG/JSON-LD формируются общими средствами, отсутствующие материал/покрытие/размер в Product не выдумываются. Проверены локальные ссылки/ресурсы и один H1. Переобход, публичный HTTPS и фактическая индексация новых карточек неприменимы до выпуска. Отчёт: `docs/verification/20260922-catalog-drafts.json`.

## Проверка релиза LOOP — 22 сентября 2026

Статика `20260922T070621Z-52722`, код `6eedb20`: единственное изменение публичного кода — передача `source_path` формой главной. Тексты, дизайн, ссылки и метаданные не менялись. Генераторы/prepare-public/check-site прошли для десяти страниц; все 222 файла релиза совпали по SHA-256. Главная, каталог, B2B, JS, robots/sitemap проверены по публичному HTTPS с проверкой сертификата, без блокирующего X-Robots-Tag. Четыре проверенных редиректа сохранили QA query. Новых/существенно изменённых страниц нет: отправка на переобход неприменима; фактическая индексация заново не проверялась. Ограничения браузерной проверки описаны в `docs/verification/20260922-loop-leads.json`.

Обновлено 14 сентября 2026. Изменения опубликованы на основном домене `https://needle-shark.ru/` 11 сентября 2026. Релиз и фактические результаты проверки индексации зафиксированы в PROJECT_CONTEXT.md.

## Единый источник метаданных

`site_utils.prepare_html()` устанавливает canonical и вызывает общий `common_metadata()` для каждой страницы. Он удаляет только принадлежащие ему прежние метатеги и создаёт ровно один набор заново. Повторный запуск не меняет результат. Все новые страницы каталога/блога получают те же правила из общих генераторов.

Для каждой страницы предусмотрены:

- один понятный title и отдельное description на русском языке;
- canonical основного HTTPS-домена без `.html`/`index.html`;
- `index,follow,max-image-preview:large`;
- Open Graph: название, описание, тип, адрес, язык, бренд и существующее изображение;
- Twitter Card с теми же данными;
- общий набор SVG/PNG/ICO-фавиконов, Apple Touch Icon и manifest;
- `Organization`, `WebSite` и `WebPage` с постоянными связанными `@id`.

На главной title конкретизирует пошив чехлов и технических изделий в Санкт-Петербурге. Слоган в первом экране сохранён. В каталоге description описывает реальный способ обращения и больше не обещает доступную покупку на маркетплейсах без товарных ссылок. Юридические документы получили отдельные description; их текст не переписывался. Уже существовавшие самостоятельные названия разделов размечены H2 с прежним внешним видом.

| Страница | Дополнительная структурированная информация |
| --- | --- |
| Главная | Бренд, сайт, компания и Санкт-Петербург, подтверждённый email и VK |
| Каталог | CollectionPage, ItemList только видимых товаров в их фактическом порядке, BreadcrumbList |
| Товар | Product: название, описание, изображения, материал, покрытие, цвет, категория и реальные размеры конкретного товара; BreadcrumbList |
| Блог | CollectionPage, ItemList только опубликованных статей, BreadcrumbList |
| Статья | BlogPosting: название, описание, дата публикации, реальная дата изменения при наличии, издатель, ссылки на использованные источники; BreadcrumbList |
| Документы | WebPage с собственными названиями и адресами |

Product описывает изделие, но **пока не соответствует требованиям товарного расширенного результата Google**: нет подтверждённой цены/Offer либо отзыва о конкретном товаре. Цены, остатки, SKU/GTIN, сроки доставки и рейтинг товара не выдумываются. Цифры рейтинга магазина на главной не превращаются в рейтинг чехла. После появления подтверждённых цен нужно отдельно обновить видимый текст, данные и разметку.

У статьи не добавляется вымышленный автор или иллюстрация. Дата изменения заполняется только при содержательной редактуре. Организация связана с существующим логотипом и контактами; полный адрес, телефон и часы работы не заявляются, поскольку не подтверждены.

## Содержание для ответов и агентов

Описания, размеры, характеристики, таблицы, FAQ, статья и источники присутствуют в исходном статическом HTML. Они читаются без выполнения JavaScript. Вопросы товара раскрываются нативным `details`; данные не подгружаются скрытым API. В статье сохранён самостоятельный краткий ответ во введении, смысловые H2/H3, списки и первичные источники рядом с соответствующими советами.

Структурированные данные описывают тот же продукт и содержание, которое видит человек. Нельзя добавлять скрытые ключевые слова, мнимые отзывы, неподтверждённую совместимость или специальные инструкции для LLM. `llms.txt`, метатеги «AI-ready» и специальные схемы сами по себе не являются условием попадания в AI-ответы Google; они не добавлены как обещание индексации. Дополнительные требования конкретных систем стоит проверять по их официальной документации, когда появится конкретная интеграция.

SEO/AEO не гарантирует обход, сроки индексации, позиции или цитирование. Проверка валидности HTML/JSON-LD подтверждает техническую готовность, а не фактическое присутствие в индексе.

## Обход и URL

`ops/prepare-public.py` создаёт robots.txt и sitemap.xml из готовых данных. В карте находятся главная, каталог, товар, блог, опубликованные статьи и два документа. Черновики не входят. `lastmod` добавлен только статье из реального поля `updated`/`date`; дата сборки не выдаётся за дату изменения текста.

В robots.txt публичный сайт разрешён для всех роботов, `/api/` исключён как нетекстовый служебный endpoint. Это правило не заменяет защиту API и не меняет существующие серверные права. Ресурсы, шрифты и фавиконы доступны для обхода. Внутренние ссылки документов и index.html нормализуются с сохранением query/fragment. Внешние ссылки сохраняют ранее согласованные UTM, внутренние не получают UTM.

Три алиаса домена уже перенаправляются на основной на VPS. Новых серверных редиректов в этом этапе не устанавливалось. Адреса с `.html` и `index.html` могут технически отвечать 200, но canonical и внутренние ссылки указывают на основной вариант; это не заявление об обязательном 301 для всех вариантов путей.

## Фавиконы и изображения

Квадратный значок сделан из существующего красного `n` логотипа без новой типографики: favicon.svg, favicon-96.png, favicon.ico (16/32/48), apple-touch-icon.png (180), icon-192.png и icon-512.png. Адреса фавиконов стабильны. `brand-preview.png` — существующий логотип на фирменном фоне для ссылок; новая иллюстрация не генерировалась.

Исходные PNG/JPG сохранены. `ops/optimize-images.py` создаёт WebP-версии шириной 160/480/800/1200/1536 px без увеличения исходника. `ops/image-manifest.json` хранит реальные размеры, SHA-256 оригинала и объёмы. Новый товар автоматически включается по `products.json`; после добавления/замены исходника нужно повторить оптимизацию. Главная использует те же подготовленные файлы; при замене её изображения обновить srcset/preload.

У изображений есть alt, реальные width/height, srcset/sizes. У первого изображения товара и главной высокий приоритет; остальные загружаются по мере приближения к экрану. Галерея меняет и src, и srcset; миниатюры используют 160 px. У исходного чехла на главной исправлены декларации 1536×1024 на фактические 900×1124, при этом его кадрирование сохраняет прежний контейнер 3:2.

## Проверка и последующие действия

Перед выпуском: генераторы → `ops/prepare-public.py` → `ops/check-site.py` и существующие тесты → браузер на пяти ширинах. `check-site.py` проверяет H1, уникальность id, локальные ссылки/якоря/ресурсы, метаданные, JSON-LD, sitemap и CSS-токены. Он не отправляет заявки и не вызывает сторонние системы.

После каждого выпуска проверять HTTPS, ресурсы и заголовки реального сайта; при существенном обновлении отправлять sitemap/важные URL на переобход в Google Search Console и Яндекс Вебмастере из аккаунта владельца. Результаты текущей проверки кабинетов — в PROJECT_CONTEXT.md. На главной сохранены публичные метатеги подтверждения Google и Яндекса: не удалять их при генерации и публикации. Затем оценивать реальные показы, страницы в индексе, ошибки обхода и Core Web Vitals. Результаты Chrome и уменьшение файлов не подменяют полевые метрики.

На VPS текущий `Cache-Control: no-cache` требует ревалидации ресурсов; сжатие текстовых ответов и длительный кеш лучше отдельно настроить после чтения/резервирования живой конфигурации и выбора стратегии версий файлов. В этой задаче Nginx, systemd, API и базы не менялись. Домен счётчика исправлен 13 сентября; подтверждение находится в docs/ANALYTICS.md.

## Официальные источники

- [Google: AI features and your website](https://developers.google.com/search/docs/appearance/ai-features) — индексируемый полезный текст, ссылки, согласованная структурированная информация; специальных AI-файлов не требуется.
- [Google: Product snippets](https://developers.google.com/search/docs/appearance/structured-data/product-snippet) — для расширенного результата нужны Offer/review/aggregateRating с настоящими данными.
- [Schema.org: Product](https://schema.org/Product) — свойства описания изделия.
- [Google: favicon](https://developers.google.com/search/docs/appearance/favicon-in-search) — квадратный доступный значок и стабильный адрес.
- [Google: Article](https://developers.google.com/search/docs/appearance/structured-data/article) — разметка публикаций.

Источники по AI, favicon и Product прочитаны 11.09.2026; руководство Article уже использовалось при создании блога. Нормы и возможности поисковых систем могут изменяться.

## Каталог из трёх товаров — 14 сентября 2026

В готовой карте сайта девять URL, включая отдельные страницы моточехла и чехлов для колёс. Все девять страниц имеют canonical на needle-shark.ru и index,follow,max-image-preview:large, доступны внутренними ссылками и содержат статические описания. На главной сохранены оба метатега подтверждения поисковых кабинетов. robots.txt разрешает публичные страницы и ресурсы, закрывая /api/. `ops/check-site.py` проверил метаданные, структурированные данные, ресурсы, ссылки и sitemap; окончательная проверка HTTPS и заголовков выполняется после публикации. Фактическое включение новых URL в индекс отдельно не подтверждено.

Публикация 14 сентября: HTTPS и совпадение девяти HTML с локальной версией подтверждены, X-Robots-Tag отсутствует. robots/sitemap/canonical, поисковые метатеги и редиректы трёх алиасов проверены на публичном сайте; для технического обхода препятствий не обнаружено. Вход в поисковые кабинеты и ручная отправка новых URL на переобход в этом выпуске не подтверждены.

## B2B — 18 сентября 2026

Публичная `/business/` входит в sitemap из десяти URL и доступна из общей навигации. HTTPS 200, canonical, index/follow, OG/JSON-LD, robots и отсутствие X-Robots-Tag проверены после установки релиза `20260918T083901Z-35018`. Google Search Console живой проверкой подтвердил доступность и возможность индексирования; запрос индексации и повторная отправка sitemap приняты. Яндекс принял переобход страницы и sitemap. Новая страница до запроса отсутствовала в индексе Google; включение в индекс не подтверждено. Исторические числа обработанных страниц в кабинетах ещё не описывают обновлённую карту.
